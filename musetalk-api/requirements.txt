# musetalk-api runtime. Assumes the MuseTalk conda env is already active
# (it provides torch, opencv, diffusers, transformers, mmengine etc).
# Install these on top of MuseTalk/requirements.txt.

fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
pydantic>=2.5
